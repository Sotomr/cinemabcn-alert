# Copia a .env y rellena (no subas .env al repositorio)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Zona horaria para "hoy / mañana / pasado mañana" (por defecto Madrid)
# TIMEZONE=Europe/Madrid

# Añadir al mensaje un bloque "novedades" respecto al snapshot anterior (1 sí, 0 no)
# APPEND_NOVELTIES=1

# Máx. títulos por cine en "Otras carteleras" (Maldà, Texas…); el resto se resume con "y N más"
# DIGEST_MAX_UNSCHEDULED=15

# Máx. películas por día en Verdi con horario (0 = sin límite). Ej. 22 si el mensaje es muy largo
# DIGEST_MAX_VERDI_PER_DAY=0

# Pie con instrucciones de debug al final del mensaje (1 sí, 0 no)
# DEBUG_FOOTER=0

# Opcional: no enviar a Telegram (solo log + snapshot)
# SKIP_TELEGRAM=1
# DRY_RUN=1

# Opcional: URL base del Phenomena (por defecto se usa phenomena-experience.com/cartelera)
# PHENOMENA_BASE_URL=https://phenomena-experience.com
# Zumzeig: por defecto se usa el calendario /es/cine/calendari/ (sesiones con fecha/hora)
# ZUMZEIG_CARTELERA_URL=https://www.zumzeigcine.coop/es/cine/calendari/
# Mooby Balmes: cartellera embebuda a window.shops (per defecte moobycinemas.com/balmes)
# MOOBY_BALMES_URL=https://www.moobycinemas.com/balmes

# TMDb: nota media + enlace IMDb en cada título (clave gratis en https://www.themoviedb.org/settings/api)
# Búsqueda: similitud de título + variantes es/en/ca; caché data/tmdb_cache_v8.json
# TMDB_API_KEY=
# Máx. películas a enriquecer por ejecución (prioridad: sesiones hoy/mañana)
# TMDB_MAX_FILMS=200
# Mínimo de votos en TMDb para mostrar nota (default 1: estrenos suelen tener pocos votos en TMDb antes que en IMDb; sube a 5 si prefieres ocultar hasta tener más consenso)
# TMDB_MIN_VOTES=1

# Digest: resumen global + cartelera por cine (hoy/mañana)
# Telegram: 1 mensaje con resumen + 1 mensaje por cine (0 = un solo mensaje como antes)
# DIGEST_TELEGRAM_BY_CINEMA=1
# DIGEST_GLOBAL_TOP=10
# DIGEST_TOP_PER_CINEMA=0
# DIGEST_EXTRA_UNRATED=5
# DIGEST_NOVELTIES_TOP_PER_CINEMA=5
# DIGEST_NOVELTIES_MAX_LINES=15
